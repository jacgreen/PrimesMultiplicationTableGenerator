#!/usr/bin/env bash

# a simple rake wrapper to make running the actual assignment trivial.  the
# 'lower level' rake task can be used to build tables of arbitrary size.
rake coding_exercises:generate_primes_multiplication_table[10]
